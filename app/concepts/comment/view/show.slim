= cell(Comment::Cell::Discussion, collection: comments)

- unless comments.last_page?
  .center.aligned#next
    = link_to "<i class='comment icon'></i> #{t(:load_more)}", next_comments_path(model: model.class.name, id: model.id, page: comments.next_page), class: "ui alliance button", remote: true, data: { turbolinks: false }


