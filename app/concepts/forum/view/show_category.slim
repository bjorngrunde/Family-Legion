.thread.actions.right.aligned

  = link_to t(:create_thread), forum_new_thread_path(forum_category_id: model.id), class: "ui tiny alliance button"

table.ui.inverted.selectable.table
  thead
    tr.dark-box
      th
        = model.title
      th
        small= t(:forum_views)
      th
        small= t(:comments)
      th
        small= t(:author)
  tbody
    = cell(Forum::Cell::Threads, collection: context[:threads])
.dark-box.center.aligned
  = paginate context[:threads] , :views_prefix => "familylegion/view"

