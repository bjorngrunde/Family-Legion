.ui.message
  i.ui.close.icon
  .header
    = t(:something_went_wrong)
  ul.list
    - context[:errors].each do |error, message|
      li
        p
          = "#{field(error)}: #{error_message(message[0])}"
